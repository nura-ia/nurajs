# __APP_NAME__

This project was bootstrapped with `create-nura` using the Node template.

## Available scripts

- `pnpm dev` – run the script in watch mode.
- `pnpm build` – emit a production bundle.
- `pnpm start` – run the compiled output.

## What's inside

- TypeScript runtime script
- `@nura-js/core` for intent definitions

## Next steps

Run `pnpm dev` to see intents in action from the terminal.
